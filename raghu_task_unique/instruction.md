You are given a broken Python script.

The script reads values from /app/input.json and computes an average.

However:
- It fails with mixed data types
- It produces incorrect output
- Output format is incorrect

Your task:
1. Fix data handling issues
2. Ensure only valid numeric values are used
3. Fix calculation logic
4. Output must be written to /app/result.json in correct format

Do not modify test files.